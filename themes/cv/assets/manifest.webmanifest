{
  "short_name": "{{ .Title }}",
  "name": "{{ .Title }}",
  "description": "{{ with site.Params.author }}{{ . }}{{ end }} - {{ .Title }}",
  "background_color": "{{ site.Params.themecolor }}",
  "display": "minimal-ui",
  "scope": "/",
  "theme_color": "{{ site.Params.themecolor }}"
}
